- Dans LD ajouter "Dprime" dans les mesures (je refuse qu'on déprime !!)
  dans snpVector.h pour le LD on divise par nbi... compter les données complètes ?

- les fonctions de lecture ddans DataStruct ne sont pas robustes
  à la présence de chaines de caractères dans une colonne numerique (eg 'NA')
  même strtof ne doit pas faire ce qu'on voudrait dans ce cas

- JU dans l'extraction de matrices, setIndStats met indStatsComputed = true
  il faudrait propager la valeur de indStatsComputed_ de la matrice originelle
  au cas où on ait seulement chargé le fam file, ou même rien chargé du tout
  /!\ Vérifier si toujours vrai !

  JU question : à quoi servent les .get() dans Column.h ? à un autre moment
  on a utilisé les shared pointeurs sans mettre de get

  JU question de retour : Pourquoi dans extractIndsfromDosagematrixMemory
  on a un template <typename SNPvectorClass (...)> pour other et newMat ?
  Dans extractIndsfromDosagematrixDisk, la f° n'est plus templatée et other
  et newMat sont des SNPmatrix<SNPdosage> (forcée)

  JU encore question de retour : Pourquoi les extracts utilisent les méthodes
  push_back() et pas les constructeurs par copie avec des keep ?

- chr_type_ dans SNPvector n'est pas utilisé dans le calcul des stats... revoir.
  [est bien utilisé pour ne calculer la GRM que sur les autosomes]

POUR AVRIL : 

- *** Niveau Débutant ***
  Interface avec houba faite, améliorer les perfs, très lente...
  Possiblement dans GRM.h déplacer usefloat en premier
          
- *** Niveau expert / Ingénieur de recherche ***
  Adapter la classe DataStruct en R (probablement S4) pour garder un *DataStruct
  va simplifier grandement le add de column (plus besoin d'en faire un par type)

- *** Niveau maso ***
  faire le tour des TODOs dans les fichiers et soit clean soit les reports ici